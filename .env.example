# Local development
# Use a PostgreSQL connection string.
# Example: postgresql://user:password@localhost:5432/esdatabase
DATABASE_URL=

# CORS
# Use a specific origin in production (e.g. https://www.example.com)
CORS_ORIGIN=*

# SSL
# Set to true/1 to disable SSL locally if needed
PGSSL_DISABLE=1

# Optional tuning
PGPOOL_MAX=10
PGPOOL_IDLE_TIMEOUT_MS=30000
PGPOOL_CONN_TIMEOUT_MS=2000
