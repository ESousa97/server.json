{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "start": "json-server -p $PORT -w db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
